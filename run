[ $# -eq 0 ] && exit 1

root=$PWD

if [ "$1" = "stop" ]; then
    exec "$root/build/ui" stop "$root/extensions/$2/build/$2.so"
fi

xmake -P "extensions/$1"
exec "$root/build/ui" "$root/extensions/$1/build/$1.so" $2
